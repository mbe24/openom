// @generated by scripts/gen-error-codes.mjs from error-codes.json — DO NOT EDIT.

/** The closed error-code registry (domain/retriable/action/args) — the client + UI mirror. */
export const ERROR_CODES = Object.freeze({
  below_gc_floor: Object.freeze({ domain: 'sync', retriable: false, action: 'bootstrap', args: Object.freeze([]) }),
  quota_exceeded: Object.freeze({ domain: 'sync', retriable: false, action: 'upgrade', args: Object.freeze([{ name: 'limit', type: 'u64' }, { name: 'used', type: 'u64' }]) }),
  rate_limited: Object.freeze({ domain: 'sync', retriable: true, action: 'retry', args: Object.freeze([]) }),
  version_conflict: Object.freeze({ domain: 'sync', retriable: true, action: 'retry', args: Object.freeze([]) }),
  covered_anomaly: Object.freeze({ domain: 'sync', retriable: false, action: 'contact', args: Object.freeze([]) }),
  invalid_request: Object.freeze({ domain: 'sync', retriable: false, action: 'contact', args: Object.freeze([]) }),
  access_denied: Object.freeze({ domain: 'sync', retriable: false, action: null, args: Object.freeze([]) }),
  not_found: Object.freeze({ domain: 'sync', retriable: false, action: null, args: Object.freeze([]) }),
  unavailable: Object.freeze({ domain: 'sync', retriable: true, action: 'retry', args: Object.freeze([]) }),
  request_failed: Object.freeze({ domain: 'sync', retriable: true, action: 'retry', args: Object.freeze([]) }),
  offline: Object.freeze({ domain: 'sync', retriable: true, action: 'retry', args: Object.freeze([]) }),
  timeout: Object.freeze({ domain: 'sync', retriable: true, action: 'retry', args: Object.freeze([]) }),
  auth_required: Object.freeze({ domain: 'auth', retriable: false, action: 'reauth', args: Object.freeze([]) }),
  session_expired: Object.freeze({ domain: 'auth', retriable: false, action: 'reauth', args: Object.freeze([]) }),
  sign_in_failed: Object.freeze({ domain: 'auth', retriable: false, action: null, args: Object.freeze([]) }),
  sign_up_failed: Object.freeze({ domain: 'auth', retriable: false, action: null, args: Object.freeze([]) }),
  email_taken: Object.freeze({ domain: 'auth', retriable: false, action: null, args: Object.freeze([]) }),
  wrong_passphrase: Object.freeze({ domain: 'vault', retriable: false, action: null, args: Object.freeze([]) }),
  tampered_anchor: Object.freeze({ domain: 'vault', retriable: false, action: 'contact', args: Object.freeze([]) }),
  recovery_code_invalid: Object.freeze({ domain: 'vault', retriable: false, action: null, args: Object.freeze([]) }),
  keyring_verify_failed: Object.freeze({ domain: 'vault', retriable: false, action: 'contact', args: Object.freeze([]) }),
  decrypt_failed: Object.freeze({ domain: 'vault', retriable: false, action: 'contact', args: Object.freeze([]) }),
  storage_quota: Object.freeze({ domain: 'storage', retriable: false, action: 'contact', args: Object.freeze([]) }),
  storage_blocked: Object.freeze({ domain: 'storage', retriable: false, action: 'contact', args: Object.freeze([]) }),
  storage_corrupt: Object.freeze({ domain: 'storage', retriable: false, action: 'contact', args: Object.freeze([]) }),
  worker_unavailable: Object.freeze({ domain: 'app', retriable: false, action: 'upgrade', args: Object.freeze([]) }),
  internal: Object.freeze({ domain: 'app', retriable: false, action: 'contact', args: Object.freeze([]) }),
});

/** Every valid code string, for guard checks. */
export const CODES = Object.freeze(Object.keys(ERROR_CODES));

/** The Fluent key for a code: `${domain}-err-${code}`. */
export function fluentKey(code) {
  const m = ERROR_CODES[code];
  return m ? `${m.domain}-err-${code}` : 'app-err-internal';
}

