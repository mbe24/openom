node_modules/
dist/
target/
apps/src-tauri/gen/
.DS_Store

# Per-machine runner config (scripts/cargo.mjs); .env.example is tracked.
.env

plan/

# Stray local pnpm store (host artifact); the real store lives in a Docker volume.
.pnpm-store/
