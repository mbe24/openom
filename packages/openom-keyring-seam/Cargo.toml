[package]
name = "openom-keyring-seam"
description = "openom — the engine-agnostic keyring swap seam: MembershipView + the keyless KeyringVerifier trait, shared by the chain and dag keyrings (OPE-276/277)"
version.workspace = true
license.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
openom-roles.workspace = true
serde = { workspace = true, features = ["derive"] }

[dev-dependencies]
serde_json.workspace = true
