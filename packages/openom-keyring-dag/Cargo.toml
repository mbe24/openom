[package]
name = "openom-keyring-dag"
description = "openom — wires openom roles/signing/keys onto the generic keyeo group-membership DAG (sequencer-free keyring, OPE-137)"
version.workspace = true
license.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
keyeo.workspace = true
blobstore.workspace = true
openom-sign.workspace = true
openom-roles.workspace = true
serde.workspace = true
serde_json.workspace = true

[dev-dependencies]
# The differential-oracle test drives the legacy CAS keyring (chain.rs) alongside the DAG one and
# asserts they agree ("CAS = degenerate DAG"). Test-only — keyeo and the keyring layer stay clean.
openom-keyring.workspace = true
openom-protocol.workspace = true
proptest.workspace = true
tempfile = "3"
