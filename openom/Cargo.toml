[package]
name = "openom"
version = "0.1.0"
description = "openom — server: the tree over S3, same DocStore contract"
license = "AGPL-3.0-or-later"
edition = "2021"
rust-version = "1.77.2"

[dependencies]
openom-store = { path = "../openom-store" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
